from django.db import models

# class ModelPostName(models.Model):
#     ign = models.CharField(max_length=100,on_delete=models.CASCADE)
#     role = models.CharField(max_length=100)
#     content = models.TextField(max_length=400)
